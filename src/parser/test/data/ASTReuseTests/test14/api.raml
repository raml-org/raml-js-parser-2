#%RAML 1.0
title: Example API
version: v1
mediaType: application/json
uses:
  types: types.raml
traits:
  params: !include paramsTrait.raml
  type:
resourceTypes:
  searchableCollection:
   get:
     is:  [params]
     responses:
       200:
         body:
           application/json:
             type: <<objectName | !singularize>>
/rest:
  get:
    responses:
      200:
        body:
          application/json:
            type: object
            properties:
              length: string
  type: {searchableCollection: {objectName: collection}}
/res:
  post:
  /{resId}:
    get:

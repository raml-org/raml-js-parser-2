#%RAML 0.8
---
title: Valid, included schema, sub-schema ref with filename.

schemas:
  - petSchema: !include ../schema/pet.json
  - petsSchema: !include ../schema/pets-filename.json

/pets:
  get:
    description: List of pets
    responses:
      200:
        body:
          application/json:
            schema: petsSchema
            example: !include ../examples/valid-pets.json

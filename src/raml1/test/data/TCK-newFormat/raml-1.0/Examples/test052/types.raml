#%RAML 1.0 Library
types:
  url:
    type: string
    pattern: (https?:\/\/(?:www\.|(?!www))[^\s\.]+\.[^\s]{2,}|www\.[^\s]+\.[^\s]{2,})
  Feature:
    type: object
    properties:
      id: number
      title: string
      description?: string
      pic: url